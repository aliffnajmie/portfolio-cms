import { Github, Linkedin, Mail, MapPin } from "lucide-react";
import { ContactForm } from "@/components/portfolio/contact-form";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import type { SiteProfile } from "@/types/profile";
export function ContactSection({ profile }: { profile: SiteProfile | null }) { return <section id="contact" className="section-shell"><div className="mb-10 max-w-3xl"><p className="eyebrow">Contact</p><h2 className="section-title">Have a useful idea? Let&apos;s talk.</h2>{profile?.availability_message && <p className="mt-5 leading-7 text-muted-foreground">{profile.availability_message}</p>}</div><div className="grid gap-6 lg:grid-cols-[.7fr_1.3fr]"><Card className="h-fit"><CardContent className="p-6 sm:p-8"><h3 className="text-lg font-semibold">Contact details</h3><div className="mt-6 space-y-4 text-sm text-muted-foreground">{profile?.availability_status && <p className="text-primary">{profile.availability_status}</p>}{profile?.location && <p className="flex items-center gap-3"><MapPin size={17}/>{profile.location}</p>}{profile?.email && <a className="flex items-center gap-3 transition hover:text-foreground" href={`mailto:${profile.email}`}><Mail size={17}/>{profile.email}</a>}</div><div className="mt-7 flex gap-2">{profile?.github_url && <Button size="icon" variant="outline" asChild><a href={profile.github_url} target="_blank" rel="noreferrer" aria-label="GitHub"><Github/></a></Button>}{profile?.linkedin_url && <Button size="icon" variant="outline" asChild><a href={profile.linkedin_url} target="_blank" rel="noreferrer" aria-label="LinkedIn"><Linkedin/></a></Button>}</div>{!profile && <p className="mt-5 text-sm text-muted-foreground">Public contact details are temporarily unavailable.</p>}</CardContent></Card><Card><CardContent className="p-6 sm:p-8"><ContactForm startedAt={Date.now()}/></CardContent></Card></div></section>; }
