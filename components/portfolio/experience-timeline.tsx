import { AlertCircle, BriefcaseBusiness, ExternalLink, MapPin } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import type { Experience } from "@/types/content";
const format = (date: string) => new Intl.DateTimeFormat("en", { month: "short", year: "numeric", timeZone: "UTC" }).format(new Date(`${date}T00:00:00Z`));
export function ExperienceTimeline({ experiences, error }: { experiences: Experience[]; error: boolean }) { return <section id="experience" className="section-shell"><div className="grid gap-12 lg:grid-cols-[.72fr_1.28fr]"><div><p className="eyebrow">Experience</p><h2 className="section-title">Design sensitivity. Engineering discipline.</h2><p className="mt-5 max-w-md leading-7 text-muted-foreground">A career shaped by practical delivery, continuous learning, and care for the people using the product.</p></div>{error ? <Card className="border-destructive/25"><CardContent className="flex min-h-48 flex-col items-center justify-center text-center"><AlertCircle className="text-red-300"/><p className="mt-4 text-sm text-muted-foreground">Experience is temporarily unavailable.</p></CardContent></Card> : experiences.length === 0 ? <Card className="border-dashed"><CardContent className="flex min-h-48 flex-col items-center justify-center text-center"><BriefcaseBusiness className="text-primary"/><p className="mt-4 text-sm text-muted-foreground">Experience will appear here once published.</p></CardContent></Card> : <div className="relative space-y-10 before:absolute before:bottom-3 before:left-[5px] before:top-3 before:w-px before:bg-border">{experiences.map((item, index) => <article key={item.id} className="animate-enter relative pl-9" style={{ "--delay": `${index * 100}ms` } as React.CSSProperties}><span className="absolute left-0 top-2 z-10 size-[11px] rounded-full border-2 border-background bg-primary shadow-[0_0_16px_hsl(var(--primary)/.5)]"/><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h3 className="text-xl font-semibold">{item.position}</h3>{item.is_current && <Badge>Current</Badge>}</div><p className="mt-1 text-primary">{item.company_url ? <a href={item.company_url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1.5 hover:underline">{item.company}<ExternalLink size={13}/></a> : item.company}</p></div><p className="shrink-0 font-mono text-xs text-muted-foreground">{format(item.start_date)} — {item.is_current ? "Present" : item.end_date ? format(item.end_date) : "Unspecified"}</p></div><div className="mt-3 flex flex-wrap gap-3 text-xs text-muted-foreground">{item.location && <span className="inline-flex items-center gap-1"><MapPin size={13}/>{item.location}</span>}{item.employment_type && <span>{item.employment_type}</span>}</div>{item.summary && <p className="mt-5 leading-7 text-muted-foreground">{item.summary}</p>}{item.achievements.length > 0 && <ul className="mt-5 space-y-2 text-sm leading-6 text-muted-foreground">{item.achievements.map((achievement) => <li key={achievement} className="flex gap-3 before:mt-2.5 before:size-1 before:shrink-0 before:rounded-full before:bg-primary">{achievement}</li>)}</ul>}{item.technologies.length > 0 && <div className="mt-5 flex flex-wrap gap-2">{item.technologies.map((technology) => <Badge variant="secondary" key={technology}>{technology}</Badge>)}</div>}</article>)}</div>}</div></section>; }
