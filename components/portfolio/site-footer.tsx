import Link from "next/link";
import { BrandMark } from "./site-header";
import { getSiteProfile } from "@/lib/profile-public";
export async function SiteFooter() { const { profile } = await getSiteProfile(); return <footer className="border-t border-border/70 bg-card/30"><div className="page-shell flex flex-col gap-6 py-8 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between"><Link href="/" className="text-foreground"><BrandMark name={profile?.full_name}/></Link><p>© {new Date().getFullYear()} {profile?.full_name ?? "Portfolio"}. Built with intention.</p><div className="flex gap-6"><Link className="transition hover:text-foreground" href="/projects">Projects</Link>{profile?.email && <a className="transition hover:text-foreground" href={`mailto:${profile.email}`}>Email</a>}</div></div></footer>; }
