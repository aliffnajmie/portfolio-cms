import { MapPin } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import type { SiteProfile } from "@/types/profile";
export function AboutSection({ profile }: { profile: SiteProfile | null }) { if (!profile?.about_bio) return null; const paragraphs = profile.about_bio.split(/\n\s*\n/).map((paragraph) => paragraph.trim()).filter(Boolean); return <section id="about" className="section-shell"><div className="grid gap-10 lg:grid-cols-[.6fr_1.4fr]"><div className="animate-enter"><p className="eyebrow">About</p><h2 className="section-title">A little more context.</h2>{profile.location && <p className="mt-5 inline-flex items-center gap-2 text-sm text-muted-foreground"><MapPin size={15}/>{profile.location}</p>}{profile.availability_message && <Badge variant="secondary" className="mt-5 whitespace-normal py-2 leading-5">{profile.availability_message}</Badge>}</div><div className="animate-enter space-y-6 text-lg leading-8 text-muted-foreground" style={{ "--delay": "120ms" } as React.CSSProperties}>{paragraphs.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</div></div></section>; }
