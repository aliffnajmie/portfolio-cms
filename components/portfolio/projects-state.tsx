import { AlertCircle, FolderKanban } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
export function ProjectsState({ variant = "empty", featured = false }: { variant?: "empty" | "error"; featured?: boolean }) { const error = variant === "error"; return <Card className={error ? "border-destructive/25" : "border-dashed"}><CardContent className="flex min-h-64 flex-col items-center justify-center px-6 py-12 text-center"><div className={`grid size-11 place-items-center rounded-md border ${error ? "border-destructive/30 bg-destructive/10 text-red-300" : "border-primary/25 bg-primary/10 text-primary"}`}>{error ? <AlertCircle size={20}/> : <FolderKanban size={20}/>}</div><h3 className="mt-5 text-lg font-semibold">{error ? "Projects are temporarily unavailable" : featured ? "Featured work is coming soon" : "No published projects yet"}</h3><p className="mt-2 max-w-md text-sm leading-6 text-muted-foreground">{error ? "Please refresh the page or try again shortly." : featured ? "New case studies are being prepared. Explore the project archive in the meantime." : "Published work will appear here as soon as it is ready."}</p></CardContent></Card>; }
