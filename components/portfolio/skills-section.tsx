import { AlertCircle, Sparkles } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import type { Skill } from "@/types/content";

export function SkillsSection({ skills, error }: { skills: Skill[]; error: boolean }) { const groups = skills.reduce<Record<string, Skill[]>>((result, skill) => { (result[skill.category] ??= []).push(skill); return result; }, {}); return <section id="skills" className="border-y border-border/70 bg-card/35"><div className="section-shell"><p className="eyebrow">Capabilities</p><div className="mb-12 grid gap-6 lg:grid-cols-2 lg:items-end"><h2 className="section-title">From product thinking to production code.</h2><p className="max-w-xl leading-7 text-muted-foreground lg:justify-self-end">A focused toolkit for creating digital products that feel considered on the surface and remain dependable underneath.</p></div>{error ? <Card className="border-destructive/25"><CardContent className="flex min-h-48 flex-col items-center justify-center text-center"><AlertCircle className="text-red-300"/><p className="mt-4 text-sm text-muted-foreground">Skills are temporarily unavailable.</p></CardContent></Card> : skills.length === 0 ? <Card className="border-dashed"><CardContent className="flex min-h-48 flex-col items-center justify-center text-center"><Sparkles className="text-primary"/><p className="mt-4 text-sm text-muted-foreground">Skills will appear here once published.</p></CardContent></Card> : <div className="cascade-grid grid gap-4 md:grid-cols-2 lg:grid-cols-3">{Object.entries(groups).map(([category, items], index) => <Card key={category} className="animate-enter" style={{ "--delay": `${index * 80}ms` } as React.CSSProperties}><CardContent className="p-6"><span className="font-mono text-xs text-primary">{String(index + 1).padStart(2, "0")}</span><h3 className="mt-6 text-xl font-semibold">{category}</h3><div className="mt-5 flex flex-wrap gap-2">{items.map((skill) => <Badge key={skill.id} variant="secondary" className="font-normal">{skill.name}</Badge>)}</div></CardContent></Card>)}</div>}</div></section>; }
