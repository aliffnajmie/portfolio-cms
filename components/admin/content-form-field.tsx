import { Label } from "@/components/ui/label";
export function ContentFormField({ name, label, hint, error, children, required }: { name: string; label: string; hint?: string; error?: string[]; children: React.ReactNode; required?: boolean }) { const describedBy = [hint && `${name}-hint`, error?.length && `${name}-error`].filter(Boolean).join(" ") || undefined; return <div className="space-y-2"><Label htmlFor={name}>{label}{required && <span className="ml-1 text-primary" aria-hidden="true">*</span>}</Label>{hint && <p id={`${name}-hint`} className="text-xs text-muted-foreground">{hint}</p>}<div data-describedby={describedBy}>{children}</div>{error?.[0] && <p id={`${name}-error`} role="alert" className="text-sm text-red-300">{error[0]}</p>}</div>; }
