"use client";
import { useActionState, useRef } from "react";
import { LoaderCircle, Trash2, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { initialMessageActionState, type MessageActionState } from "@/types/contact";
type Action = (state: MessageActionState, data: FormData) => Promise<MessageActionState>;
export function MessageDeleteButton({ action }: { action: Action }) { const [state, formAction, pending] = useActionState(action, initialMessageActionState); const dialog = useRef<HTMLDialogElement>(null); return <div><Button type="button" variant="destructive" onClick={() => dialog.current?.showModal()}><Trash2/>Delete message</Button><dialog ref={dialog} className="w-[calc(100%-2rem)] max-w-md rounded-lg border border-border bg-card p-0 text-card-foreground shadow-2xl backdrop:bg-black/70"><div className="flex justify-between gap-4 border-b border-border p-5"><div><h2 className="text-lg font-semibold">Delete message?</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">This message and its private notes will be permanently removed.</p></div><Button type="button" variant="ghost" size="icon" aria-label="Close dialog" onClick={() => dialog.current?.close()}><X/></Button></div><form action={formAction} className="flex flex-col-reverse gap-3 p-5 sm:flex-row sm:justify-end"><Button type="button" variant="outline" onClick={() => dialog.current?.close()}>Cancel</Button><Button type="submit" variant="destructive" disabled={pending}>{pending ? <><LoaderCircle className="animate-spin"/>Deleting...</> : <><Trash2/>Delete permanently</>}</Button></form></dialog>{state.error && <p role="alert" className="mt-3 text-sm text-red-300">{state.error}</p>}</div>; }
